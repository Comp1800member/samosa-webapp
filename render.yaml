services:
  - type: web
    name: samosa-webapp
    runtime: node
    plan: free
    # App lives in the webapp/ subfolder
    buildCommand: cd webapp && npm install
    startCommand: cd webapp && npm start
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_VERSION
        value: "20"
